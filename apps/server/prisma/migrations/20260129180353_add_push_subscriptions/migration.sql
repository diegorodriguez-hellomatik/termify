-- AlterEnum
-- This migration adds more than one value to an enum.
-- With PostgreSQL versions 11 and earlier, this is not possible
-- in a single migration. This can be worked around by creating
-- multiple migrations, each migration adding only one value to
-- the enum.


ALTER TYPE "NotificationType" ADD VALUE 'TERMINAL_CRASHED';
ALTER TYPE "NotificationType" ADD VALUE 'SSH_CONNECTION_FAILED';
ALTER TYPE "NotificationType" ADD VALUE 'VIEWER_JOINED';
ALTER TYPE "NotificationType" ADD VALUE 'VIEWER_LEFT';
ALTER TYPE "NotificationType" ADD VALUE 'COMMAND_COMPLETED';

-- CreateTable
CREATE TABLE "push_subscriptions" (
    "id" TEXT NOT NULL,
    "user_id" TEXT NOT NULL,
    "endpoint" TEXT NOT NULL,
    "p256dh" TEXT NOT NULL,
    "auth" TEXT NOT NULL,
    "user_agent" TEXT,
    "terminal_crashed" BOOLEAN NOT NULL DEFAULT true,
    "ssh_connection_failed" BOOLEAN NOT NULL DEFAULT true,
    "viewer_activity" BOOLEAN NOT NULL DEFAULT true,
    "command_completed" BOOLEAN NOT NULL DEFAULT true,
    "share_notifications" BOOLEAN NOT NULL DEFAULT true,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "push_subscriptions_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "push_subscriptions_endpoint_key" ON "push_subscriptions"("endpoint");

-- CreateIndex
CREATE INDEX "push_subscriptions_user_id_idx" ON "push_subscriptions"("user_id");

-- AddForeignKey
ALTER TABLE "push_subscriptions" ADD CONSTRAINT "push_subscriptions_user_id_fkey" FOREIGN KEY ("user_id") REFERENCES "users"("id") ON DELETE CASCADE ON UPDATE CASCADE;
